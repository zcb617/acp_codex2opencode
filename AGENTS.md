# workflow

**交付测试，必须使用本机真实环境，不要使用受限的容器环境**

## 铁律
1. 任何适合，汇报完成前，都必须通过交付测试，严格禁止未通过交付测试，就向用户汇报已完成开发
2. 交付测试前，必须通过自动化测试

## 基本要求

1. 交付的代码必须通过交付测试。
2. 交付测试必须使用 Codex CLI 的真实用户入口发起。
3. 交付测试必须按照真实业务链路执行，不能只做代码测试、函数测试或字段检查。
4. 如果交付测试失败，不能声明完成；必须修复后重新执行同一条真实业务链路。

## 方案与计划文档要求

每次 Bug 修改、功能新增或业务流程调整，在进入实施前都必须先形成可恢复上下文的方案或计划文档。

文档不是临时聊天记录，而是上下文压缩后仍能恢复当前任务的业务与技术依据。

### Bug 修改

Bug 修改必须按照 `docs/可交付BUG修改设计文档编写指南-v0.1.md` 编写设计文档，并保存到 `docs/superpowers/specs/`。

Bug 修改设计文档必须明确：

- 当前要解决的业务问题。
- 真实失败现象、用户输入、实际表现和预期表现。
- 根因分析和证据。
- 本次修复目标与非目标。
- 修改范围、风险与回退路径。
- 需要修改的主要模块或文档。
- 自动化验证目标。
- 交付测试目标：必须写清真实入口、真实业务语言、同一失败链路的复测方式、通过标准和失败后继续整改闭环。
- 当前任务推进到哪里，下一步应做什么。

Bug 修改进入实施前，还必须按照 `docs/可交付BUG修改计划编写指南-v0.1.md` 编写 Bug 修改计划，并保存到 `docs/superpowers/plans/`。

Bug 修改计划必须承接对应设计文档，明确：

- 设计目标覆盖表：设计中的每个修复目标都必须映射到实施任务、自动化验证和交付测试验证。
- 实施任务拆分：每个任务必须说明业务目标、设计来源、修改范围、实施步骤、验证方式和完成标准。
- TDD 与红灯测试计划：核心失败必须先被测试捕获，再进入修复。
- 自动化验证计划：必须覆盖精准回归、相关模块、全量测试、构建或安装检查。
- 真实业务交付测试计划：必须从真实入口开始，使用真实业务语言，复测同一失败链路或等价业务链路。
- 失败整改与再计划机制：交付测试失败后必须记录失败事实、补充整改任务、重新验证并重新执行真实业务交付测试。
- 设计完成核对清单：确认设计文档中承诺的内容已经全部完成。

### 功能新增和业务流程调整

功能新增和业务流程调整必须按照 `docs/可交付开发计划编写指南-v0.1.md` 编写可交付开发计划，并保存到 `docs/superpowers/plans/`。

可交付开发计划必须同时覆盖：

- 要开发什么、怎么开发、改哪些文件。
- 业务交付场景。
- 交付完成定义。
- 自动化验证命令。
- 真实业务交付测试目标。
- 失败修复与复测机制。
- 上下文压缩后的恢复说明。

没有对应的 Bug 修改设计文档或可交付开发计划，不允许直接开始编码实施。

## 业务导向交互原则

任何面向用户的输出都必须以业务为导向，而不是以代码、接口、字段或内部状态为导向。

业务导向是所有项目工作的基本原则，不只适用于交付测试。凡是涉及用户交互、流程提示、等待说明、错误说明、选择项、验收反馈、文档说明和最终汇报，都必须先回答用户真正关心的业务问题：

- 当前处于哪个业务阶段。
- 为什么进入这个阶段。
- 现在需要用户做什么业务选择。
- 这个选择会怎样影响后续业务流程。
- 当前结果是否已经满足业务目标。

禁止把内部实现语言作为用户主提示，例如：

- `workflow_status`
- `current_stage`
- `next_action_required`
- MCP 工具名
- API 字段名
- 内部状态枚举

这些信息只能作为调试信息在必要时补充，不能作为首屏主表达。用户默认看到的应该是业务语言，例如：

```text
当前已经有了《方案》和《计划》，按约定可以直接进入计划实施阶段。请为本次计划实施选择执行模型。
```

阶段判断必须遵循业务语义：

1. 没有方案时，进入方案制定。
2. 已有方案但没有计划时，进入计划制定。
3. 已有方案和计划，并且用户确认可以执行时，进入计划实施。
4. 只有计划实施阶段才需要选择 ACP 执行模型。
5. 方案制定和计划制定默认由主会话执行，不要求用户选择 ACP 模型。

## 交付测试定义

交付测试不是验证某个函数、字段或内部工具调用是否正确，而是验证：这个项目交给最终使用者后，能否按照真实业务目标完整跑通。

交付测试的判断标准不是“代码已经实现”、不是“自动化测试通过”、也不是“接口有返回”，而是：

- 用户从真实入口开始使用。
- 用户使用真实业务语言表达需求。
- 系统按设计目标完成完整流程。
- 过程中没有卡住、绕路、误触发、旧逻辑残留或设计以外的现象。
- 用户能理解当前状态，并能继续推进任务。

## 当前项目的真实业务入口

本项目是 Codex 插件，真实业务入口不是内部 API，也不是直接调用某个 MCP 工具。

真实入口必须包含：

1. 安装当前插件。
2. 重启或刷新 Codex 使用环境。
3. 打开 Codex CLI。
4. 在 Codex CLI 中输入自然语言业务需求。
5. 让 Codex 根据插件、技能和项目规则自行完成委派流程。

如果某个改动会影响安装、加载、技能触发、委派流程或用户可见输出，这些环节都必须纳入交付测试。

## 当前项目的交付测试链路

交付测试必须使用worktree，避免污染项目代码。
使用Codex CLI 和 gpt-5.3-Codex 模型 完成交付测试，必须覆盖当前项目的完整业务闭环：

1. 插件可以被安装。
2. Codex 可以加载插件能力。
3. 用户用自然语言可以触发团队委派流程。
4. 当前开发计划/开发方案中列出的交付测试项

**及其重要：docs/团队委派交付测试必过表.md 这份文档中的测试项，必须再每一次交付测试中通过，只要有一项不通过就算为交付测试失败** 

## 测试语言要求

交付测试必须使用真实用户会说的话。

可以使用类似表达：

```text
帮我用团队委派流程完成这个开发任务。
```

```text
设计和计划已经确认，直接进入实施。
```

```text
过程中有进展就告诉我，没动静太久再问我是否接手。
```

不能使用类似表达：

```text
调用 MCP 工具。
```

```text
调用 delegate_task_execute。
```

```text
检查 progress_update 字段。
```

原因：真实用户不会用内部实现语言来操作产品。

## 禁止作为交付测试的做法

以下做法不能算作交付测试：

- 直接调用内部 API。
- 直接调用 MCP 工具。
- 使用临时 MCP 参数绕过插件安装。
- 伪造 ACP 状态或事件。
- 只检查返回字段，不观察用户可感知结果。
- 只运行单元测试、集成测试或自动化脚本。
- 跳过安装、加载、启动或真实 CLI 使用入口。
- 用开发者指令代替真实用户需求。

这些方式可以作为辅助验证，但不能替代交付测试。