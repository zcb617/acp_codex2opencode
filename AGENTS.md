# workflow

**交付测试，必须使用本机真实环境，不要使用受限的容器环境**

## 基本要求

1. 交付的代码必须通过交付测试。
2. 交付测试必须使用 Codex CLI 的真实用户入口发起。
3. 交付测试必须按照真实业务链路执行，不能只做代码测试、函数测试或字段检查。
4. 如果交付测试失败，不能声明完成；必须修复后重新执行同一条真实业务链路。

## 业务导向交互原则

任何面向用户的输出都必须以业务为导向，而不是以代码、接口、字段或内部状态为导向。

业务导向是所有项目工作的基本原则，不只适用于交付测试。凡是涉及用户交互、流程提示、等待说明、错误说明、选择项、验收反馈、文档说明和最终汇报，都必须先回答用户真正关心的业务问题：

- 当前处于哪个业务阶段。
- 为什么进入这个阶段。
- 现在需要用户做什么业务选择。
- 这个选择会怎样影响后续业务流程。
- 当前结果是否已经满足业务目标。

禁止把内部实现语言作为用户主提示，例如：

- `workflow_status`
- `current_stage`
- `next_action_required`
- MCP 工具名
- API 字段名
- 内部状态枚举

这些信息只能作为调试信息在必要时补充，不能作为首屏主表达。用户默认看到的应该是业务语言，例如：

```text
当前已经有了《方案》和《计划》，按约定可以直接进入计划实施阶段。请为本次计划实施选择执行模型。
```

阶段判断必须遵循业务语义：

1. 没有方案时，进入方案制定。
2. 已有方案但没有计划时，进入计划制定。
3. 已有方案和计划，并且用户确认可以执行时，进入计划实施。
4. 只有计划实施阶段才需要选择 ACP 执行模型。
5. 方案制定和计划制定默认由主会话执行，不要求用户选择 ACP 模型。

## 交付测试定义

交付测试不是验证某个函数、字段或内部工具调用是否正确，而是验证：这个项目交给最终使用者后，能否按照真实业务目标完整跑通。

交付测试的判断标准不是“代码已经实现”、不是“自动化测试通过”、也不是“接口有返回”，而是：

- 用户从真实入口开始使用。
- 用户使用真实业务语言表达需求。
- 系统按设计目标完成完整流程。
- 过程中没有卡住、绕路、误触发、旧逻辑残留或设计以外的现象。
- 用户能理解当前状态，并能继续推进任务。

## 当前项目的真实业务入口

本项目是 Codex 插件，真实业务入口不是内部 API，也不是直接调用某个 MCP 工具。

真实入口必须包含：

1. 安装当前插件。
2. 重启或刷新 Codex 使用环境。
3. 打开 Codex CLI。
4. 在 Codex CLI 中输入自然语言业务需求。
5. 让 Codex 根据插件、技能和项目规则自行完成委派流程。

如果某个改动会影响安装、加载、技能触发、委派流程或用户可见输出，这些环节都必须纳入交付测试。

## 当前项目的交付测试链路

交付测试必须覆盖当前项目的完整业务闭环：

1. 插件可以被安装。
2. Codex 可以加载插件能力。
3. 用户用自然语言可以触发团队委派流程。
4. 设计和计划确认后，可以进入实施阶段。
5. 实施阶段可以正确调用 ACP。
6. ACP 有进展时，主会话能输出简短、可理解的进展总结。
7. ACP 持续有输出时，不应反复询问用户是否继续等待或是否主会话接手。
8. ACP 连续沉默超过设计阈值后，才询问用户是否继续等待或主会话接手。
9. 用户选择继续等待后，进入新的持续跟进周期。
10. 等待过程中 ACP 再次返回内容时，必须清空旧的沉默计时，并恢复正常进展总结。
11. 最终能完成任务，或给出符合设计预期的失败说明和接手路径。

## 测试语言要求

交付测试必须使用真实用户会说的话。

可以使用类似表达：

```text
帮我用团队委派流程完成这个开发任务。
```

```text
设计和计划已经确认，直接进入实施。
```

```text
过程中有进展就告诉我，没动静太久再问我是否接手。
```

不能使用类似表达：

```text
调用 MCP 工具。
```

```text
调用 delegate_task_execute。
```

```text
检查 progress_update 字段。
```

原因：真实用户不会用内部实现语言来操作产品。

## 禁止作为交付测试的做法

以下做法不能算作交付测试：

- 直接调用内部 API。
- 直接调用 MCP 工具。
- 使用临时 MCP 参数绕过插件安装。
- 伪造 ACP 状态或事件。
- 只检查返回字段，不观察用户可感知结果。
- 只运行单元测试、集成测试或自动化脚本。
- 跳过安装、加载、启动或真实 CLI 使用入口。
- 用开发者指令代替真实用户需求。

这些方式可以作为辅助验证，但不能替代交付测试。

## 通过标准

只有同时满足以下条件，才可以认为交付测试通过：

- 从安装到真实使用入口的链路可以跑通。
- Codex CLI 中的自然语言请求可以触发正确业务流程。
- 委派流程符合设计目标。
- ACP 实施阶段的进展展示符合用户体验要求。
- 沉默超时询问逻辑符合设计目标。
- 用户没有被不必要地打断。
- 没有出现卡住、绕路、误触发或旧逻辑输出。
- 最终结果对用户而言是可理解、可继续、可交付的。

## 失败判定

出现以下任一情况，都必须判定交付测试失败：

- 插件安装失败。
- Codex 无法加载插件能力。
- 自然语言请求无法触发预期流程。
- 必须使用内部工具名才能触发流程。
- ACP 有输出但主会话没有合理总结。
- ACP 持续有输出时仍反复询问用户是否接手。
- ACP 已经恢复输出，但旧的沉默计时仍继续生效。
- 流程卡住且没有符合设计的接手路径。
- 实际用户体验与设计目标不一致。

交付测试失败后，必须记录失败位置、用户输入、实际表现、预期表现和修复结果；修复后重新执行完整交付测试。
